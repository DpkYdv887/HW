// Prompt the user for their current age
let age = parseInt(prompt("Please enter your current age:"));

// Use a ternary operator to check if the age is 18 or older
let message = (age >= 18) ? "You're an adult" : "You're just a baby";

// Display the alert message
alert(message);
